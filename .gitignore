# dependencies / build output
node_modules/
.next/
dist/
*.tsbuildinfo

# env — ห้าม commit secret (ดู SECURITY.md) — ใช้ .env.example เป็นแบบ
.env
.env.local

# รูปแนบเคสตอน dev (ฟีเจอร์ถูกถอด 20 ก.ค. 2026 — กันไฟล์เก่าที่ค้างในเครื่อง dev หลุดเข้า git)
apps/api/uploads/

# AI tool workspace
.kilo/
