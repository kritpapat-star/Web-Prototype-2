-- ลบตาราง ticket_images — เลิกเก็บ metadata รูปแนบใน DB (20 ก.ค. 2026)
-- รูปจะย้ายไปเก็บที่อื่น (ที่เก็บใหม่ยังไม่กำหนด) — ฟีเจอร์แนบรูปถูกถอดออกทั้งชุด
-- ⚠️ ไฟล์รูปเดิมใน volume `erp_uploads` ไม่ถูกลบโดย migration นี้ แต่จะไม่มี metadata ชี้ถึงอีกต่อไป
DROP TABLE "ticket_images";
